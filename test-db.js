const { Client } = require('pg'); const client = new Client('postgresql://postgres:postgres@127.0.0.1:54322/postgres'); async function run() { await client.connect(); await client.query('DROP TABLE IF EXISTS leads_v2, leads_v2_archive, kanban_cards_v2, kanban_cards_v2_archive, labels_v2, lead_labels_v2, hospedin_room_mappings, hospedin_settings;'); console.log('Dropped!'); await client.end(); } run().catch(console.error);
